<UserControl x:Class="XrayDetector.Gui.Views.ScenarioRunnerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Scenario Runner" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <ComboBox Grid.Row="1" ItemsSource="{Binding Scenarios}" SelectedItem="{Binding SelectedScenario}" DisplayMemberPath="Name" Margin="0,0,0,10"/>

        <ProgressBar Grid.Row="2" Height="25" Value="{Binding Progress}" Maximum="100" Margin="0,0,0,10"/>

        <Button Grid.Row="3" Content="Execute Scenario" Command="{Binding ExecuteCommand}" HorizontalAlignment="Left" Width="150" VerticalAlignment="Top"/>

        <TextBlock Grid.Row="3" Text="{Binding Result.Message}" TextWrapping="Wrap" Margin="0,40,10,10" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
