<UserControl x:Class="XrayDetector.Gui.Views.SimulatorControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel Margin="10">
            <TextBlock Text="Simulator Control" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <GroupBox Header="Panel Parameters" Margin="0,0,0,10">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Rows:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PanelRows, Mode=TwoWay}" Margin="0,0,0,5"
                             AutomationProperties.AutomationId="InputRows"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Cols:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PanelCols, Mode=TwoWay}" Margin="0,0,0,5"
                             AutomationProperties.AutomationId="InputCols"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="kVp (40-150):" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding PanelKvp, Mode=TwoWay}" Margin="0,0,0,5"
                             AutomationProperties.AutomationId="InputKvp">
                        <TextBox.ToolTip>
                            <ToolTip>
                                <StackPanel>
                                    <TextBlock FontWeight="Bold" Text="kVp"/>
                                    <TextBlock Text="Range: 40–150 kV"/>
                                    <TextBlock Text="X선관 가속 전압 - 높을수록 투과력 증가"/>
                                </StackPanel>
                            </ToolTip>
                        </TextBox.ToolTip>
                    </TextBox>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="mAs:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding PanelMas, Mode=TwoWay}"
                             AutomationProperties.AutomationId="InputMas">
                        <TextBox.ToolTip>
                            <ToolTip>
                                <StackPanel>
                                    <TextBlock FontWeight="Bold" Text="mAs"/>
                                    <TextBlock Text="Range: 1–1000 mAs"/>
                                    <TextBlock Text="X선 노출량 - 높을수록 신호 강도 증가"/>
                                </StackPanel>
                            </ToolTip>
                        </TextBox.ToolTip>
                    </TextBox>
                </Grid>
            </GroupBox>

            <GroupBox Header="Network Parameters" Margin="0,0,0,10">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Packet Loss (0-1):" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PacketLossRate, Mode=TwoWay}" Margin="0,0,0,5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Frame Buffers (1-8):" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding FrameBufferCount, Mode=TwoWay}"/>
                </Grid>
            </GroupBox>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Load Config" Command="{Binding LoadConfigCommand}" Width="100" Margin="0,0,5,0"
                        AutomationProperties.AutomationId="BtnLoadConfig"/>
                <Button Content="Save Config" Command="{Binding SaveConfigCommand}" Width="100" Margin="0,0,5,0"
                        AutomationProperties.AutomationId="BtnSaveConfig"/>
                <Button Content="Start" Command="{Binding StartCommand}" Width="80" Margin="0,0,5,0"
                        AutomationProperties.AutomationId="BtnStart"/>
                <Button Content="Stop" Command="{Binding StopCommand}" Width="80" Margin="0,0,5,0"
                        AutomationProperties.AutomationId="BtnStop"/>
                <Button Content="Reset" Command="{Binding ResetCommand}" Width="80"
                        AutomationProperties.AutomationId="BtnReset"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
